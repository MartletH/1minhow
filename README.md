# 1minhow

Public workspace for lightweight publishing and MCP payload experiments.

The `mcp-payload-lab` branch is reserved for connector payload verification. It must not contain secrets or production credentials.
